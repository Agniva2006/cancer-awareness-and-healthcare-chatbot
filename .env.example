# Groq API Key (required for LLM inferences)
GROQ_API_KEY=your_groq_api_key_here

# JWT Secret Key for user session tokens (optional - generated automatically if omitted)
ONCOGRAPH_SECRET_KEY=your_secure_random_hex_secret

# Local Server Port (Render sets this dynamically via $PORT)
PORT=8000
